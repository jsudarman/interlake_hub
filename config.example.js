// Supabase configuration — TEMPLATE FILE (safe to commit)
// ─────────────────────────────────────────────────────────────────────────────
// 1. Copy this file:  cp config.example.js config.js
// 2. Open config.js and replace the placeholder strings with your real values.
// 3. config.js is gitignored — never commit the real key.
//
// Variable names:
//   SUPABASE_URL      — your project URL  (Settings → API → Project URL)
//   SUPABASE_ANON_KEY — your anon/public key (Settings → API → anon public)
// ─────────────────────────────────────────────────────────────────────────────

var SUPABASE_URL      = 'https://YOUR_PROJECT_REF.supabase.co';
var SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY_HERE';
